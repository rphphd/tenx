'use strict';

describe('Directive: masterUpdate', function () {

  // load the directive's module
  beforeEach(module('re2App'));

  var //element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

/*  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<master-update></master-update>');
    element = $compile(element)(scope);
    expect(element.text()).toBe('this is the masterUpdate directive');
  })); */
});
